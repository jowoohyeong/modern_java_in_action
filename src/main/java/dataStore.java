import DTO.Dish;

import java.util.Arrays;
import java.util.List;

public class dataStore {
    List<Integer> numbers = Arrays.asList(1, 2, 1, 3, 5, 6, 9, 43, 44, 3, 2, 4);
    int[] number2 = {2,3,5,7,11,13};
    List<Integer> someNumbers = Arrays.asList(1, 2, 3, 5, 6);
    List<Dish> specialMenu = Dish.menu;
    List<Dish> menu = Dish.menu;
}
